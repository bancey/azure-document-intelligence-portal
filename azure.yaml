# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: document-intelligence-portal
metadata:
  template: document-intelligence-portal@0.0.1-beta

services:
  web:
    project: ./src/DocumentIntelligencePortal
    language: dotnet
    host: appservice

infra:
  provider: bicep
  parameters:
    location: uksouth
